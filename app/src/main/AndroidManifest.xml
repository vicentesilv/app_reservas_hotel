<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher_image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_image"
        android:supportsRtl="true"
        android:theme="@style/Theme.Appreservashotel">
        <activity android:name=".Registro" />
        <activity android:name=".HotelesActivity" />
        <activity android:name=".MiInformacionActivity" />
        <activity android:name=".Login" />
        <activity android:name=".HotelRooms.HotelRoom" />
        <activity android:name=".CrearReservaActivity" />
        <activity android:name=".VerReservasActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
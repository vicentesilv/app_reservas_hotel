<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <!-- Contenido principal replicando el estilo de activity_habitaciones -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/bg_gradient">

        <!-- Toolbar fijo en la parte superior y ocupando todo el ancho -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:background="@color/white"
            android:elevation="4dp"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">

            <!-- Botón flecha atrás -->
            <ImageButton
                android:id="@+id/btnBackReserva"
                android:layout_width="40dp"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_arrow_back"
                android:contentDescription="@string/back"
                app:tint="@color/black" />

            <!-- Botón menú (abre drawer) -->
            <ImageButton
                android:id="@+id/btnMenuReserva"
                android:layout_width="40dp"
                android:layout_height="match_parent"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_menu"
                android:contentDescription="@string/menu"
                app:tint="@color/black" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="@string/title_crear_reserva"
                android:textColor="@color/black"
                android:textSize="20sp"
                android:textStyle="bold" />


        </androidx.appcompat.widget.Toolbar>


        <!-- Contenido desplazable debajo del Toolbar -->
        <ScrollView
            android:id="@+id/scroll_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"
            tools:ignore="UselessParent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp">

                <!-- Mantener SearchView fuera del contenido aquí; si necesitas que esté dentro del Toolbar, se puede añadir en código -->

                <TextView
                    android:id="@+id/tvRoomHeader"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textAppearance="@style/TextAppearance.AppCompat.Large"
                    android:textColor="@color/on_surface"
                    android:textStyle="bold"
                    android:paddingBottom="12dp"
                    tools:text="Reservar: Hotel Ejemplo - Individual" />

                <!-- Reemplazado: antes había un Button (btnSelectRange). Ahora se muestra un CalendarView inline para seleccionar fechas directamente. -->

                <androidx.cardview.widget.CardView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    app:cardElevation="4dp"
                    app:cardCornerRadius="8dp"
                    app:cardUseCompatPadding="true"
                    app:cardBackgroundColor="@color/white">

                <CalendarView
                    android:id="@+id/calendarViewReserva"
                    android:layout_width="match_parent"
                    android:layout_height="350dp"
                    android:layout_marginBottom="12dp"
                    android:focusedMonthDateColor="@color/primary_variant"
                    android:selectedWeekBackgroundColor="@color/calendar_selected"
                    android:showWeekNumber="false"
                    android:weekSeparatorLineColor="@android:color/transparent" />
                </androidx.cardview.widget.CardView>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:paddingBottom="12dp">

                    <TextView
                        android:id="@+id/tvFechaEntrada"
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:layout_height="wrap_content"
                        android:padding="8dp"
                        android:background="@android:color/transparent"
                        android:textColor="@color/on_surface"
                        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                        tools:text="Entrada: 2025-11-24" />

                    <TextView
                        android:id="@+id/tvFechaSalida"
                        android:layout_width="0dp"
                        android:layout_weight="1"
                        android:layout_height="wrap_content"
                        android:padding="8dp"
                        android:background="@android:color/transparent"
                        android:textColor="@color/on_surface"
                        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
                        tools:text="Salida: 2025-11-25" />
                </LinearLayout>

                <EditText
                    android:id="@+id/etName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:hint="@string/hint_reserva_nombre"
                    android:inputType="textPersonName"
                    android:autofillHints="name"
                    android:layout_marginBottom="12dp" />

                <Button
                    android:id="@+id/btnReservar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:backgroundTint="@color/button_bg"
                    android:textColor="@color/button_text"
                    android:text="@string/btn_reservar" />

                <TextView
                    android:id="@+id/tvSelectionHelp"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/secondary_text"
                    android:paddingTop="8dp"
                    tools:text="Toque una fecha en el calendario para asignarla a Entrada o Salida. Primero toca Entrada para activarla." />

            </LinearLayout>
        </ScrollView>

    </LinearLayout>

    <!-- Navigation drawer (mismo header y menú que activity_habitaciones) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view_reserva"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header_hotels"
        app:menu="@menu/drawer_menu"
        app:itemIconTint="@color/nav_item_color"
        app:itemTextColor="@color/nav_item_color" />

</androidx.drawerlayout.widget.DrawerLayout>
